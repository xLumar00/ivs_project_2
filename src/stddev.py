#!/usr/bin/env python3
import sys

def stddev_calc(count, sum_nums, sum_sq):
    # Placeholder for TDD
    return 22.22 #random return value since 0.0 would actually be correct for some tests

def main():
    count = 0
    sumOfNumbers = 0.0
    sumOfSquares = 0.0
    for line in sys.stdin:
        print(line, end="")


if __name__ == "__main__":
    main()